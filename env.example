# 复制到 .env.local，并填入 Supabase 项目的连接字符串。
# 提示：Supabase → Project Settings → Database → Connection string → URI。
# Supabase/Postgres 连接字符串，供数据库和 ORM 使用。
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/postgres?sslmode=require"
# Better Auth 用来签名/验证令牌的随机密钥。
BETTER_AUTH_SECRET="generate-a-long-random-secret"
# Next.js 对外公开地址，用于生成跳转、回调等绝对链接。
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# 微信开放平台应用的 AppID。
WECHAT_APP_ID=""
# 微信开放平台应用的 AppSecret。
WECHAT_APP_SECRET=""
# 微信接口返回的语言标识（cn/en）。
WECHAT_LANG="cn"
# 为无邮箱的微信用户生成临时邮箱时使用的域名。
WECHAT_SYNTHETIC_EMAIL_DOMAIN="wechat.local"
# 是否打开微信登录相关的调试日志（true/false）。
WECHAT_DEBUG="false"

# 通用 S3 兼容存储配置（AWS S3、R2、MinIO 等）。
# 存储访问密钥 ID。
S3_ACCESS_KEY_ID=""
# 存储访问密钥 Secret。
S3_SECRET_ACCESS_KEY=""
# 存储区域（Region）。
S3_REGION=""
# 用于存放对象的桶名称。
S3_BUCKET_NAME=""
# 可选自定义 Endpoint（例如 https://s3.amazonaws.com 或 R2 URL）。
S3_ENDPOINT=""
# 读取文件时使用的公共基础 URL（留空则退回 AWS 样式）。
S3_PUBLIC_URL=""
# 某些提供商（如 MinIO）需要路径风格请求时设为 true。
S3_FORCE_PATH_STYLE="false"
